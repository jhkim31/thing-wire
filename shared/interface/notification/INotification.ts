/**
 * Notification 인터페이스
 * ```typescript
 * interface INotification {
 *  name: string;
 *  id: string;
 *  url: string;
 * }
 * ```
 */
export default interface INotification {
    name: string,
    id: string,
    url: string
}