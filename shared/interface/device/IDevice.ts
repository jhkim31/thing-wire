/**
 * 장치에 대한 Interface
```typescript
interface IDevice {
    id: string;
    name: string;    
    type: number;
    comment: string;
}
```
 */
export default interface IDevice {
    id: string;
    name: string;    
    type: number;
    comment: string;
}